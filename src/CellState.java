/**
 * This enum dictates the states of each Cell. It can use it's state to dictate its behavior and the enum stores
 * what state a cell is at.
 */
public enum CellState{
        ALIVE,
        DEAD,
        WILL_DIE,
        WILL_REVIVE;
}
